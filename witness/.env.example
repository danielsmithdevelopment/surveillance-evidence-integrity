# Copy to .env for local EAS / expo run (never commit secrets)
EXPO_PUBLIC_CTF_API=https://challengethefootage.com
EXPO_PUBLIC_CTF_WEB=https://challengethefootage.com
# Enable after `npm run whisper:fetch` + custom native build (not Expo Go):
# EXPO_PUBLIC_WHISPER=1
